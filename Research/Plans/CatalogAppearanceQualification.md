# APPEAR-C — catalog and independent-appearance qualification

## Integrated family evaluation tranche — 2026-09-23

User assigned larger continuation: source-backed family compatibility, all retained
nine-pair intake, frame/crop review, overlap accounting, fixed-threshold comparison
of shipped/FDR-007/FDR-008, and grouped next-stage requirements. No training, capture,
producer edits or promotion. Read local Git object cda0a32 without fetch/checkout;
the older working tree is not the only locally available producer source.

Appearance-v1 adds blank_placeholder/high_contrast/photos_like. Optional family_id
is derived as appearance-v1.<preset>.<layout> and excluded from the canonical hash.
Missing/null matches Swift decodeIfPresent; conflicting/nonstring values reject.
Unknown fields still reject. Preserve observation brackets and original values;
aliases must agree. Pin source/doc hashes and externally recorded recipe hashes.

Review all nine pairs and18 production crops, preserving development seed lineage.
Once native labels/crops validate, run authorized diagnostic scoring even if the
separate independent-holdout criteria are unmet. Freeze membership/model/runtime and
threshold0.85 before inference; report backend timing, misses, false positives and
abstentions separately. No tuning or scoring-derived ground truth. Test actual
intake/crop entrypoints and legacy/adversarial contracts, then full offline checks
once. Deliver ranking/coverage and the grouped next-stage proposal, not just a patch.

Include the previously admitted ten appearance pairs in the same fixed comparison;
keep the separate catalog quarantine untouched. For new focused frames compare all
annotated controls, including disabled negatives, under existing seven box variants.
This is classification sensitivity, not action/navigation eligibility. Freeze an
expanded development-only B1 proposal with the nine newly reviewed pairs, preserving
the original proposal and missing evaluation/selection/approval blockers.

## Appearance-v1 consumer continuation — 2026-09-23

Assigned continuation: implement the optional appearance-v1 recipe contract in
existing v2 intake and production-crop paths. No capture, training, producer edits
or model scoring. Pin TTR e3d55d1, `Docs/CLI/fixture-appearance-v1.md`, shared
`FixtureAppearance.swift`, and three `Scripts/fixture-appearance-vectors.json` hashes.

Admit integer version1, presets artwork/bright_unfocused/gray_placeholder,
media_shelf standard and grid_matrix standard/dock. Missing/null retains legacy
hashes; append the documented suffix after randomization when present. Consumer
policy rejects extra appearance fields as unbound semantics (Swift decoding may
ignore them). Unknown versions/values/combinations also fail; no guessed defaults.
V2 bracket, pair and flat-alias equality must bind the complete recipe. Unversioned
sidecars with appearance cannot supply those brackets and must reject it.

Preserve appearance in existing observationBinding recipes through normalized and
v1.5 crop manifests; no schema/public API change or new cropper. Seed grouping
stays independent of preset/layout. Source-byte revalidation rejects altered derived
bindings. Legacy manifests gain no required fields.

Acceptance: independent producer vectors; legacy/null hash parity; all supported
combinations; malformed/unknown fields; dropped/changed appearance across brackets,
pairs and aliases; cross-partition siblings; real ingest/crop CLI and existing
preflight tests. Run required offline Swift build/test once at integrated handoff.
Genuine intake additionally requires the retained10-pair archive (116122181 bytes,
SHA256 `d80f14887c399e85c738ba3705096fc4ad6d116d37a38c5ca36d872919b5764a`)
delivered locally, then full byte/label/crop/lineage review. Missing delivery blocks
that criterion only. No images on SMB; no recapture of completed producer output.
Preserve prior catalog reservation and independently report evaluation eligibility.

## Original catalog assignment

Assigned2026-09-23 by maintainer: complete corrected catalog qualification and
varied-appearance independent-evaluation readiness in one tranche. No training,
Office, producer edits, installation, restart or model promotion.

Use existing app-managed Fixture jobs, bundled chunk export, strict sidecar-v2
consumer and production16%/256 cropper. Exact local simulator9026ECA9-77DB-4AE6-8FE6-BB239E9571FA;
fresh ownership, installed identities and listener binding before capture. Normal
app/simulator runtime storage permitted for this existing lane; explicit consumer
outputs remain project-local. At most three serialized jobs, each bounded10 minutes,
one recipe per job; failure/ambiguous cleanup stops collection without retry.

Frozen matrix before acquisition: kitchen_sink/41 catalog entries/regular/step0,
seed307, themes light,dark,high_contrast. Expected per recipe:8 real annotations,
4 native-observed focus targets,33 excluded placeholders;4 pairs and5 unique
capture states. Total12 pairs/15 capture states. Recipe count41 is not41-class
training coverage. Verify exact targets from current source and actual observations.

Reserve all three theme variants together in candidate-evaluation quarantine;
do not train, split by seed/theme or score models yet. Existing v1.5 derived
manifest remains development-pilot eligibility; a separate hash-bound reservation
records intended use, not an automatic independent-holdout approval. Audit all raw
and crop decoded hashes against APPEAR-B training/validation/protected evidence,
including transitive lineage; new seeds or zero pixel matches alone do not prove
semantic independence. All catalog siblings stay together.

Deliver source/build pins, complete counts, producer exclusions, independently
verified export hashes, visual review of each theme/control and every anomaly,
real consumer intake, crop geometry, postflight health and immutable reservation.
Report actual visual differences; do not count theme labels as different images.
Assess all five APPEAR-B reserved strata and their two-independent-groups-per-split
minimum. Unsupported renderer axes or insufficient independent families remain
explicit blockers, with a bounded producer request; never relax the requirement.
Top Shelf status is not in-app artwork or native Home focus-label qualification.

Acceptance: qualified catalog source or concrete failed-boundary evidence; coverage
and leakage report, independent-evaluation decision and exact missing capability
handoff. Full training readiness requires independent validation/final challenge,
reviewed protocol and separate candidate authorization. Do not substitute this
small catalog for6000 pairs, physical qualification or all six model gates.

Observed intake correction: first genuine four-target catalog passes byte/sidecar
validation but fails crop admission because existing `destructiveButton` is omitted
from the extraction focusable set. Include this existing button taxonomy case;
require the same native observed labels and v2 bracket checks. No new category or
permission to activate destructive controls. Regression covers real crop CLI plus
continued rejection of focused noninteractive labels. Reuse captured bytes.
