# Worker knowledge routing

**Reviewed:** 2026-09-19. This is an evidence index and uncertainty register, not a second best-practices document or task queue. Proven lessons remain in BestPractices.md. Recheck mutable facts at execution time.

| ID | Finding and evidence | Worker consequence / resolution |
|---|---|---|
| K-01 | `eval_phase6a.py:WEIGHTS_DIR` defaults to `phase6a_r007`; recorded baseline is Run 009. | P1 must require or explicitly pass/check/hash Run 009 weights. Never infer checkpoint from report title. BP-50. |
| K-02 | `train_ios_model.py:parse_args/main`: dry-run trains 2 epochs at 5%, disables warmup, changes rect/mosaic; `--resume` resumes state. | P5 separates validation-only preflight, fresh fine-tuning, and smoke training. BP-51. |
| K-03 | ExperimentLog.md already records numeric Runs 010–012 and FDR-001. | P5 reserves no numeric ID now; read log and allocate immediately before execution. |
| K-04 | Tasks.md and `reports/tvtestrig_feedback_2026-09-18.md` document IPC resolved, identity preflight blocked. | Live capture remains unavailable until identity and Office access are satisfied; no offline worker should retry it. CurrentState/FixtureBatchIngest reconciled in this workflow update. |
| K-05 | Fixture ingest source documents scale=1 vs annotation schema's 2/3 restriction. | P4 produces a versioned compatibility decision before emitting claims of schema-valid output. |
| K-06 | Category map includes stepperControl and no badge. Maintainer resolved the planning ambiguity on 2026-09-19 in DeliveryDecisions.md. | Current gate is toggle/stepperControl AP50 ≥0.88. Later BADGE-A/B appends ID 41 with versioned maps and badge AP50 ≥0.88. Never fabricate badge metrics for 41-class weights. |
| K-07 | nativeui-model-workflow skill contains historical blanket `.scaleFill`/Swift-evaluator guidance; AGENTS.md and architecture §9.2 specify shipped YOLO letterboxing. | Preserve production letterbox semantics. P1 evaluates PyTorch with its existing YOLO path. Historical Create ML advice is not authority to change YOLO preprocessing. Protected skill correction requires its approved edit workflow. |
| K-08 | Fixture skill counts `*_result.json` predictions; FixtureBatchIngest.md says these are not ground truth. | Prediction distributions are diagnostics only. P4 rejects them as training annotation sources. |
| K-09 | Historical capture notes require container writes and changing HOME; current repository boundary and higher-level instructions prohibit these actions here. | No capture recipe from those notes is executable by these offline packets. Resolve a compliant export workflow in a future capture assignment. |
| K-10 | CurrentState shipped table uses FocusRing `v1.0` ID while Tasks and the same snapshot call v1.0 unshipped and v0.1 bundled. | Version-label discrepancy remains unresolved. Do not change/promote the model based on a prose label; inspect packaged metadata in a separately scoped release review. |
| K-11 | 2026-09-19 manifest-driven checks: all 2,000 test links broken, plus 10,543 train and 2,696 val; labels and historical Run 009 report remain. [Evidence](../reports/dataset_availability_2026-09-19.md). Cause unknown. | P0 recovery precedes actual P1/P3 evaluation and training eligibility. Surviving reports do not imply runnable pixels; reconstructed/uncertain corpus identity needs a new baseline/version. BP-52. |
| K-12 | Read-only TVTestRig HEAD `586050e`, `Docs/Testing/harvest-bundle-validation.md`: offline validator/regression lane exists; passing result remains unverified and training-ineligible. No producer tests executed in this review. | H1/P4-A can iterate on source-pinned offline bundles before identity/hardware. P4-L independently proves genuine-bundle compatibility. See TVTestRigIntegrationContract.md. |
| K-13 | Revision-2 packets combined software delivery and real-data qualification, leaving useful tooling marked blocked by missing pixels. | Revision 4 replaces combined descriptions with canonical independent packets and four-outcome handoffs. BP-53; model/data gates remain binding. |

For a new entry: record date, observed evidence (path/symbol or report), fact versus hypothesis, affected packet, and the exact resolution test. Once resolved, link the resulting BP/decision and mark the entry superseded rather than retaining two competing instructions.
