# APPEAR-A1 — Integrated appearance pilot software

Closed `direct-tvos-appearance-catalog-v1` integrated into the existing runner,
receipt validator and v1.4 production crop intake. No new cropper, trainer or
producer wire contract. Existing42-recipe and two-element catalogs are unchanged.
[Frozen catalog](catalog.json):24 recipes,76 expected pairs,100 expected frames.
Canonical content hash (excluding its hash field):
`61c0b5c6ddd38582a533410ff3bf526da03d938b98245a58a1e445ad1083dfee`.

The [offline preflight](offline-preflight.json) passed against actual Fixture source
pins. Its endpoint is proposed, not an observed listener. No simulator was contacted.
Source presence does not identify installed builds. Counts are expected targets,
not demonstrated distinct eligible examples.

## Acceptance evidence

- Actual planning CLI emits the closed catalog and rejects output reuse.
- Current source pins are required before appearance execution. Missing/changed
  source, invalid UUID, non-loopback endpoint and occupied output fail before
  runtime binding/mutation. Existing exact-process endpoint binding remains.
- Offline mocked execution exercises all24 recipes through completion audit.
- Actual validation/crop intake CLIs produce76 test-only pairs with production
  16%/256 preprocessing. Training preflight rejects them.
- Partial membership, changed catalog/source, stale focus and corrupt pixels
  reject. Existing interval, geometry, duplicate, sidecar, version and split tests
  remain passing. Old resume assembly rejects appearance catalogs.
- Historical receipt validation does not read today's producer checkout.
- [Python tests](python-tests.log):37 pass in17.939s, exit0. Initial test fixture
  used a noncanonical receipt filename; corrected to actual `direct-capture.json`.
- [Swift build](swift-build.log):exit0,3.34s; [Swift tests](swift-test.log):exit0,
  93 tests in2.190s. No capture/intake of genuine data or external waiting occurred.

Outcomes: **software verified; data eligibility not established; live integration
not assessed; model gates not assessed.** No real capture, training, export,
promotion, Office operations or external-repository edits.

Changed code: `direct_tvos_appearance.py`, targeted `direct_tvos_capture.py`
extensions and `test_direct_tvos_appearance.py`. Updated canonical plan, queue,
roadmap, snapshot/catalog links and this handoff. Preserved pre-existing FDR-008,
visual diagnostics, APPEAR-A and unrelated worktree changes.
Shared coordination is not applicable to this local software extension. Existing
TTR rendering request remains separate; no new peer acknowledgment claimed.

## Next action

Authorize the bounded simulator pilot, freshly verify target/build/endpoint and
Fixture ownership, then capture, review and compare fixed shipped/FDR-007/FDR-008
references on identical development membership. Do not train automatically.
Home artwork/dock gaps are not solved by compact/spacious layouts.
See [runbook](runbook.md). Appearance resume is intentionally unsupported;
never merge partial results into the old42-recipe pilot.
