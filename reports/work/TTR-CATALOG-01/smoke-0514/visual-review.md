# Two-control smoke visual review

Reviewed three unique3840x2160 frames: shared unfocused reference plus each
focused button state. Both buttons remain readable and inside the frame. The
reference control has native focus in the baseline; neither dialog button does.
Authorize remains bright when unfocused, an important negative example, not an
incorrect focus label. Native observed focus, not brightness or requested focus,
supplies labels. Focused buttons visibly enlarge/change treatment. Cancel changes
from dark to light; focused Authorize differs subtly from the bright baseline.

All four crops were rendered by production makeCrop,16% expansion/256x256.
They contain the target and focus-effect extent; neighboring-button slivers are
expected context. Wide-control anisotropic distortion is existing preprocessing.
Measured nominal native bounds remain700x144 in both states; they are not a claim
that the painted focus effect has identical bounds. No frame clipping observed.

Receipt/index/PNG hashes and all four before/after scene brackets pass existing
strict validation;2 accepted rows,0 unknown classes. This tiny seed7 dialog smoke
is related development evidence only, not an independent holdout, training
approval, navigation success or a model-quality result.
