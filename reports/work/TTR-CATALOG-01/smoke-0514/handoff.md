# Genuine two-control smoke — passed through NUIAK intake

2026-09-23, authorized single action_dialog/high_contrast/regular/seed7/step0,
two elements; exact simulator9026ECA9-77DB-4AE6-8FE6-BB239E9571FA. Matching new
app/helper and Fixture5b697fb4 from preceding recheck. App/simulator runtime
storage explicitly included; caller reports, raw bundle and crops remain NUIAK-local.

| Outcome | Result |
|---|---|
| Software verified | Existing v2 validator and real crop/manifest entrypoint pass; prior8 tests reused |
| Data eligible |2 reviewed pairs admitted development-only; trainingApproval false |
| Integration qualified | Capture → supported chunk export → strict v2 intake → production crops passes for this recipe/build |
| Model gate passed | Not assessed; no training/inference/promotion |

## Execution and acceptance

- Job48683F4D-B6F7-4EE9-B852-51B4958192BA staged and started once. Recipe SHA
  212f35c0395e8482756773652b238acc78ce000af18279370c21e2ea2999ea07.
  Terminal completed:2 calibration rows,0 rejected,0 training,0 held-out. Completed
  within the10-minute limit, first status poll; no automatic retry.
- Native observed reference and both focused states passed fresh before/after
  brackets. Initial default-scene no_sample did not persist after recipe application.
- Signed `export-job` failed at export_staging_create/access_denied,
  writercli_caller, Cocoa513. Retained failure and completed job; no recapture.
  Matching app's bundled Ruby `export-fixture-job.rb` succeeded using documented
  read-only manifest/chunk IPC and caller-owned project-local output.
- All12 files independently verified against producer sizes/SHA256. Index SHA
  de6b72985315f3ff067a7bc1d50d6a30d9cae044574ff51f4af33c2c058bc739.
  Both sidecars validate as v2;0 unknown classes,4 PNGs decode,3 unique full frames.
  Source context retained as reported, not authenticated image identity.
- Visually reviewed three unique full frames and four production crops. Hash-bound
  visual-review.json links the review. Existing actual CLI `ttr_focus_manifest.py`
  exits0,2 pairs,manifestv1.5.16% expansion and256px production makeCrop unchanged.
- Raw destination `dataset/tvos_captures/ttr-dialog-20260923-0514`; derived
  `dataset/focus_ring/ttr-dialog-20260923-0514/focus_dataset_manifest.json`.
  Related seed7 examples stay development-only; not independent evaluation support.
- Postflight HTTP/device/scene responsive, unchanged Fixture instance, settled,
  reasonready, readiness ownershipclear. Fixture left on final smoke scene; no
  claim of prior-scene restoration. No NUA capture process/session left running.

Preparation/start/status/export/chunk-export/inspection/intake results retained here.
Commands use matching bundled helper and approved host context; chunk exporter,
independent inspection and intake exit0. Signed export failure is separate, not
misreported as fixed. No producer/product code edits; prior tests reused, no redundant
Swift rebuild. inspect_bundle.py is this bounded diagnostic, not a new cropper.

Whole assigned smoke/export/intake tranche complete. Catalog four-pair transfer and
Top Shelf visible rendering remain distinct work, not blockers for this qualified
dialog path. Next useful tranche: qualify corrected catalog and targeted appearance
variations, freeze independent evaluation groups, then a reviewed candidate proposal.
Do not collect thousands of duplicate dialog examples or launch training from this smoke.
