# Architecture review: allow one repaired media boundary attempt

User explicitly approved separate-copy metadata cleanup, existing-signature
verification and exact-simulator Fixture installation/relaunch. Source6093663
changes publication from layout fallback to native-measured elements, with explicit
exclusions for unrendered headers. Four target-planning source hashes are unchanged.
Old failed receipt and captured bytes remain unchanged.

Staged executable02142035/dylib46011bb0 bytes match producer originals. Initial
copy verified; FinderInfo appeared later and blocked the first pre-install check.
One authorized copy-only FinderInfo deletion immediately followed by strict
verification passed. Install and launch succeeded on simulator
9026ECA9-77DB-4AE6-8FE6-BB239E9571FA. No re-signing, host replacement or reset.
Installed PID66851 owns the bound endpoint, with expected binaries/runtime/Xcode.

Initial passive scene is zero-size/no_sample with no measured elements. This is
not positive geometry evidence. Review accepts **one bounded attempt**, not data:
configure the frozen media_shelf/light/seed7/four-card recipe, require settled fresh
native reference and all four observed focus states, validate every screenshot
interval/coordinate and postflight. Existing10-second settling/120-second recipe
bounds and no-retry policy remain. Failure preserves partial evidence and stops.

Neither source review nor installation upgrades provenance or grants training
eligibility. Only a genuine passing boundary unlocks the remaining authorized pilot.
