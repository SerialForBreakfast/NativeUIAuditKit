# Independent evaluation admission: blocked

**Superseded compatibility finding:** [integrated family tranche](../APPEAR-FAMILY-EVAL-20260923/handoff.md)
found cda0a32 in local Git objects, repaired admission and completed9-pair intake
plus19-pair development scoring. No contract transfer/rebuild is needed. Independent
holdout status remains blocked; preserve this earlier diagnostic as history only.

2026-09-23T16:08:04Z. User authorized independent evaluations, not training/capture.
Used fixture/model workflows to distinguish intact transport from eligible labels
and independent membership. No model inference was launched on unadmitted data.

## Checks completed

- Original archive retained; matching transfer receipt unchanged. Extracted into
  new `dataset/tvos_captures/appear-family-handoff-20260923/extracted` after prior
  archive path/type inventory. No supplied code executed or producer files edited.
- `audit.py` exit0: all39 indexed artifacts across three bundles match byte counts
  and SHA256; all18 PNG files fully decode. Three manifest hashes match delivered
  README; completed receipts each report3 rows. Archive contains45 bundle files
  (including index/receipt) plus README. See audit.json for exact identities.
- Actual `harvest_bundle_validation.validate_bundle` rejects each bundle with
  `invalid_metadata: appearance_fields`. No fields were stripped or labels repaired.
- Recipe appearance adds `family_id` plus new presets `high_contrast`, `photos_like`,
  `blank_placeholder`. Local TTR checkout0e43ab3 documents only the original three
  presets and three-field object. Archive README pins producer cda0a32 but supplies
  neither updated source contract nor independent recipe hash vectors. Guessing the
  new hash/field semantics from nine samples would weaken source-backed admission.

## Independent evaluation decision

All nine rows declare producer split `training`, seed7, regular density, dark theme.
Three declared family IDs are not nine independent groups. All are seed siblings
under the currently frozen cross-adapter grouping rule and seed7 already exists in
the development pool. Producer split is advisory, not proof of actual prior model
use, but cannot establish untouched status either. No actual model-use claim made.

Even after consumer compatibility is extended, this artifact alone cannot supply
two unrelated groups with both labels in each of five strata for both validation
and final challenge. Need reviewed source lineage/prior-use evidence and separately
reserved groups; do not relabel these examples or loosen the existing protocol.
No crop/full-corpus leakage qualification or visual-label acceptance is claimed:
those depend on supported semantic admission. Bytes remain available for subsequent
compatibility and development evaluation without another transfer or capture.

Software verification: existing validator executed as designed; no product code
change or new full-suite claim. Transfer integrity passed. Data eligibility blocked.
Producer-consumer integration failed at new appearance contract. Model gates not
assessed. No trained model, scores, export, promotion or simulator operations.

## Resume

TTR supplies a small source-pinned contract handoff for cda0a32: exact appearance
schema/allowed values, family-ID derivation, recipe-hash canonicalization and vectors,
relevant shared source, and family relationship/prior-use evidence. Existing approved
small-file SMB path is sufficient; no duplicate image archive needed. Then NUIAK
implements tested compatibility, validates native labels and production crops,
reviews actual rendered differences, and decides diagnostic versus independent use.
True independent comparisons remain pending qualified untouched evaluation groups.
