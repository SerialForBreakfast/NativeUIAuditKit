# Existing evidence: disposition and capture gaps

2026-09-22. Scope: exactly the supplied legacy crop manifest and flat capture directory;
not an external search. [Fresh audit](audit.json), [44-image visual review](visual-review.json).
All originals remain in place. Quarantine means excluded from qualification, not moved.

## What is usable now

- 44/44 capture PNGs decode; all were visually triaged. 29 have hash/dimension-matching
  source sidecars, but **all 29 sidecars contain zero element annotations**. 15 are
  orphan images; 11 prediction JSONs are diagnostics, never truth.
- One `office_tvos_settings_main.png` shows a Settings list with visible chevrons.
  It is an annotation lead, not the reported About → Name failure, not a focused/unfocused
  pair, and not a qualified benchmark. Focus/disabled state cannot be reconstructed
  from requested navigation or similar row appearance. Device-management text warrants
  privacy review before any separately approved sharing/use.
- Two Photos welcome images show contrasting button focus appearances. They are useful
  review leads, not verified callbacks, paired geometry or an independently held-out journey.
- The Fixture diagnostic screenshot is not a completed seeded capture bundle.
  App-switcher cards, Home grids and welcome screens must not be labeled as dialogs
  simply because they are overlays or have buttons.
- `office_pluto_loaded.png` visibly shows Photos; `office_settings_target.png` shows
  Home/Fixture artwork. Filename intent is not screen identity. `office_screen_opened.png`
  includes account identity: privacy-review quarantine, with no transcription in reports.

The 1,500-pair legacy corpus has 3,000 valid 256×256 crops, all dark. There are 84
cross-split pixel-duplicate groups and zero frame-evidence envelopes. The previous audit
reproduces exactly; neither reseeding nor automatic repartitioning repairs missing labels
or makes its historical 270/270 an independent quality result. No individual crop was
manually relabeled; all 1,500 pairs remain unqualified pending a separate reuse protocol.

## Frozen coverage/gap matrix

Counts below are **qualified** support, not apparent objects in thumbnails. Every cell
is currently zero; visual leads do not fill the gold-label requirement.

| Capability | Missing evidence required before a real baseline |
| --- | --- |
| Chevron → correct row | Reviewed pixel boxes/relations for ordinary, focused, clipped, disabled and nested rows; decorative-arrow/text negatives; About → Name incident if retained |
| Dialog → buttons/focus | Reviewed true dialogs and informational overlays, destructive confirmations, no/multiple-focus ambiguity and localized semantics; Delete Siri History incident if retained |
| Visual focus | Completed raw focused/resting frames, per-frame target geometry and settled observed callbacks, receipt/index, producer/build context; all six scene families and light/dark/high-contrast |
| Hard negatives | Verified unfocused imageView/collectionItem across light/high-contrast; final held-out ≥100 and all four combinations nonempty |
| Transition readiness | Complete ordered journeys with timestamps, arrival times, independent observed focus and reviewed ready/unstable/unknown intervals; static/crossfade/scroll/focus animation/background carousel |
| Screen/row identity | Registered screen/row references plus reviewed queries under changed values/scroll/hidden rows, duplicate labels, overlays, stale epochs and supported/unsupported locales |
| Independence | Journey/run/recipe relationships, retry/variant grouping, near-duplicate review; freeze final groups before tuning, keep known failures in development |

No new capture request is dispatched here. When TTR is repaired, the first authorized
operation remains the bounded genuine smoke, not the whole matrix. Reuse that completed
bundle for intake and geometry validation, then separately authorize the development pilot.
Do not navigate into destructive native confirmations to fill gaps: use already retained
approved evidence or separately authorized inert Fixture replicas, honestly labeled.

## Consumer handoff requirements

Provide original pixels, source sidecars and completed receipt/index with hashes; retain
producer originals. For each paired frame, preserve observed target/reference focus,
settling/freshness evidence, frame identity and actual geometry—not requested focus alone.
Keep capture failures and partial output distinct. Record source/build, dimensions,
recipe/seed/theme/group and cleanup health; do not invent missing producer fields.
Transfer images through the approved data route, **never the status share**.

These requirements consolidate existing contracts; they do not request new wire fields,
attestation machinery, VoiceOver alignment, remote execution or a new producer feature.
