# Direct pilot continuation — integrated software handoff

2026-09-22 23:06 UTC. Owner: NUIAK architect, TVGEN-03. Scope: implement/review the
previously missing multi-run capture and completion path while Fixture repair is
pending. No live mutation, screenshot, training, installation or producer edit.

| Outcome | Result |
|---|---|
| Software verified | Pass:32 Python tests; offline Swift build and14 XCTest/93 Swift Testing tests |
| Data eligible | No new admission; retained12 recipes/30 pairs re-audited, still failed-run evidence |
| Integration qualified | Test-only execution/assembly/crop/baseline integration passes; genuine remaining30 recipes/216 pairs blocked by Fixture geometry |
| Model gate passed | Not assessed; no candidate or genuine-pilot inference |

## Delivered and reviewed

- `scripts/direct_tvos_resume.py`: actual `--plan`, `--execute`, `--assemble`
  entrypoints. Ordered receipt chain pins the original42-recipe catalog, source
  plan, every preceding receipt hash and stable simulator/runtime/profile/Xcode.
  Distinct build and instance identities survive in per-run raw receipts.
- Existing capture runner executes an explicit contiguous missing range only.
  After failure, a separately prepared repair review must identify the failed
  receipt, changed replacement binaries, source-plan pins and hash-bound report.
  Preflight rebinds the current exact endpoint; native interval checks remain
  mandatory. No blind retry or automatic preparation of a passing review.
- `direct-tvos-segment-v1` cannot enter the consumer as a complete corpus.
  `direct-tvos-capture-set-v1` is produced only for complete42-slot coverage.
  Assembly copies audited indexed images, original sidecars and raw receipts into
  new per-run directories and reconstructs the combined path view during validation.
  It never changes a source failed receipt to completed. Partial copy errors retain
  output without a completed receipt. Original unindexed/failed diagnostics remain
  at their source locations; this is not a whole-run backup/export utility.
- Existing v1.4 consumer dispatches through the same validation and production
  `FocusRingClassifier.makeCrop` path. Complete-set visual review is still required
  for genuine intake. Synthetic evidence cannot become reviewed evidence; training
  preflight still refuses development data. No new cropper, trainer or public API.
- `Research/Plans/ParallelTVOSAcquisition.md` records the reviewed contract before
  implementation. Other dirty model/export/perception/iOS work preserved.

## Acceptance evidence

`PYTHONDONTWRITEBYTECODE=1` with the approved focus-export-01 Python environment:

1. `-m unittest discover -s scripts -p 'test_direct_tvos*.py'`: exit0,
   **32 tests in19.736s**. Existing20 plus12 continuation tests.
2. Tests invoke real plan/assembly/validation CLI, bounded resumed executor with
   deterministic doubles, complete42 recipes/246 pairs through existing extraction
   (492 production crops), training rejection and baseline-protocol preparation.
   Rejected cases include missing ranges, overlaps, duplicate receipts, bad hashes,
   corrupted copied PNGs/sidecars, changed projections, incompatible runtime,
   mixed evidence, partial sweeps, symlinks, test-only execution, unchanged failed
   build, changed repair report, output collision and no-retry failure retention.
   These are generated test-only images, not genuine model quality evidence.
3. Initial integration test correctly failed `duplicate_pair_content` because solid
   test images were identical across recipes. Test pixels were made deterministically
   distinct by recipe/state; production deduplication was not weakened.
4. Actual `--plan --receipts dataset/tvos_captures/direct-pilot-20260922-2152/failed.json
   --output reports/work/TVGEN-RESUME-02/retained-plan.json`: exit0.12 recipes/30 pairs
   audited;30 missing recipes/216 targets. ExecutionAllowedfalse/admittedPairs0.
   Original receipt remains SHA256
   `7e419d25e299cbdf0e7b3993729c0acf4fd71ee13e0c7896b49bb5cd76f77ff2`.
5. `verify.py` runs repository-required offline build/test with project-local
   tmp/cache/config/security and automatic resolution disabled. Both exit0;
   build5.241s/test4.338s; no warning/error lines. Commands/timings in
   `swift-verification.json`, full logs adjacent. Scoped diff whitespace check passes.

Source hashes at handoff:
- continuation `b3f92d207ba7af0ebfb0ae52800c14d64ef98f6a00ac5d507288388d371ea569`
- capture `e8d0a921151d0cd05db3a55c05c72b3c431864dadd548c1f9f3e3a764cc24f8a`

## Live blocker and next action

22:57Z passive check: PID28018 owns8080; installed Fixture dylib remains
`9bd3859b9f71e6255bba0a3d64cfdd31cad323a67d70416826330c1414fa70f0`;
`/scene` still includes the conflicting media-header bounds. ProducerHEAD3667185
is unchanged. Latest shared acknowledgment22:00Z is now expired status, not a
fresh completion report or device-availability claim. No unchanged capture retry.

Next, under existing pilot authority: inspect actual geometry repair/source pins,
verify current target/ownership, record repair review, then use `--execute` with
the **failed receipt**, not the planning JSON. Begin a one-recipe segment at index12
to qualify the repaired boundary. If successful and healthy, continue remaining
groups with all ordered predecessor receipts, assemble the complete set, inspect
family/theme/anomaly overlays and run the frozen shipped-model baseline.
No intermediate user approval is required for already authorized pilot steps;
new installation, scale collection or training still needs separate authority.

Current software deliberately rejects partial final recipes and zero-progress
segments rather than guessing which uncertain capture can be reused. Incompatible
runtime or changed target-plan pins require a reviewed contract revision. A new
binary/reviewer assertion alone never substitutes for per-frame correctness.

The direct lane does not wait for TTR's separate sidecar repair. Live capture,
visual qualification and genuine baseline are the remaining assigned criteria;
all require corrected producer geometry. No background work or polling remains.
