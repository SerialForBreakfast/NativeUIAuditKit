# Publication receipt

Verified SMB endpoint: `smb://sillycon.local/SharedStatusFile`, mounted at
`/Volumes/SharedStatusFile`. Published NUA-owned architect summary and packet
`TTR-CHECK-20260922-1622` in `/Volumes/SharedStatusFile/nuiak/status.yaml`.
Observation/publication timestamp16:25:31Z, validity ends16:55:31Z.

Scoped host write approval obtained; `apply_patch` exited0. YAML safely parsed
before/after; new packet build hash, readiness, acknowledgments checked. Content
hashes of all20 pre-existing packet entries match the pre-patch snapshot. No peer,
reservation, raw-image or credential files changed. No stale whole-file replacement.

Readback passed. **Peer acknowledgment of this update is pending.** Historical
producer acknowledgments of the native-identity request and crop repair were read
and reconciled separately, not counted as receipt of this new snapshot.

Local Tasks/CurrentState updated; documentation-only diff check passed. No software
change, build, capture or model test claimed in this diagnostic turn.

## Fixture follow-up publication

Published the restored HTTP health and fresh unresolved-focus evidence to the same
NUA-owned packet and architect summary at16:29:01Z, valid through16:59:01Z.
Re-read immediately before the targeted patch. YAML validation and readback passed;
all20 pre-existing packet hashes remain unchanged. Peer acknowledgment remains
pending. The earlier timeout is historical, not the current blocker.
